pipeline {
  agent any 
    stages {
      stage ('this is first stage '){
        steps {
          echo "this is the forst command"
          }
         }
       stage ('this is second  stage '){
        steps {
          echo "this is the forst command"
    Parameters  {
        string (name: 'plese enter your name', defaultValue: 'staging', description: 'adfgadvjgtjyhdgavu')
        }
          }
         }
        }
       }
